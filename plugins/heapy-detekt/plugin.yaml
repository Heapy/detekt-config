tasks:
  detekt:
    action: !io.heapy.detekt.plugin.runDetekt
      sources: ${module.kotlinJavaSources}
      configFile: ${pluginSettings.configFile}
      defaultConfigFile: ${project.rootDir}/detekt.yml

checks:
  - detekt
